spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/${MONGO_DB_NAME}
  jpa:
    hibernate:
      ddl-auto: update
  flyway:
    enabled: false
logging:
  level:
    org:
      springframework:
        security: DEBUG