name: it-offers ci/cd
on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    services:
      mongodb:
        image: mongo:6
        env:
          MONGO_INITDB_ROOT_USERNAME: root
          MONGO_INITDB_ROOT_PASSWORD: example
          MONGO_INITDB_DATABASE: it-offers
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --eval 'db.runCommand(\"ping\")'"
          --command "--replSet test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - uses: actions/checkout@v4

#    - name: Install mongosh
#      run: |
#        sudo apt-get install gnupg
#        wget -qO- https://www.mongodb.org/static/pgp/server-7.0.asc | sudo tee /etc/apt/trusted.gpg.d/server-7.0.asc
#        echo "deb [ arch=amd64,arm64 ] https://repo.mongodb.org/apt/ubuntu focal/mongodb-org/7.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-7.0.list
#        sudo apt-get update
#        sudo apt-get install -y mongodb-mongosh
#        mongosh --version
#
#    - name: Initalize MongoDB
#      run: |
#        mongosh --host localhost:27017 -u admin -p admin --authenticationDatabase admin <<EOF
#          db = db.getSiblingDB('APP-DB');
#          db.createUser({ user: 'APP-USERNAME', pwd: 'APP-PASSWORD', roles: [{ role: 'readWrite', db: 'APP-DB' }] })
#          db.createCollection('APP-COLLECTION');
#        EOF
#        # Do whatever you like in here

# TODO step below is rather to remove
    - name: Some random step that can be removed
      run: |
        echo "Another random step that can be removed"  

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven

    - name: Build and test with Maven
      run: mvn clean install
      env:
        JWT_SECRET: 2D4A614E645267556B58703273357638792F423F4428472B4B6250655368566D
